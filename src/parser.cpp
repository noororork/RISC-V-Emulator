// parser.cpp
// Parses lines that have been passed through and sends them to execute

#include <cstdint>
#include <vector>
#include <string>
using namespace std;

class Parser{
    public:
        void parse(string s){
            s.trim();
        }
};